# Normalise to LF in the repository. The STATE file in particular is parsed by
# first token on the first line, and a stray CR is the kind of thing that turns
# "RUN" into an unparseable verb — which, correctly, reads as YELLOW.
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary

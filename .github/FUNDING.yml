# GitHub renders this as a "Sponsor" button at the top of the repository.
#
# Delete any line whose destination does not actually exist yet — a sponsor
# button that leads nowhere is worse than no button, and per the protocol's own
# rule, a value with no live source behind it should not be shown at all.
#
#   github -> requires enabling at https://github.com/sponsors (Stripe onboarding)
#   custom -> any URL. The PayPal link below is a hosted donate button on the
#             Woods Wiring LLC account. The button id carries the amount and
#             currency settings, so no address appears in this file.

custom: ["https://www.paypal.com/donate/?hosted_button_id=KU3RLVTG3NGW8"]

# Uncomment once GitHub Sponsors is actually enabled on the account:
# github: [Solmex72]

# Uncomment if these are ever created:
# ko_fi: solmex72
# buy_me_a_coffee: solmex72
